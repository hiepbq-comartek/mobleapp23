import * as React from 'react';
import {Text, View, FlatList, ScrollView, StyleSheet} from 'react-native';
// import Video from 'react-native-video';

export default Videofriend = () => {
  return (
    <View
      style={{
        // flex: 1,
        backgroundColor: '#000',
      }}>
      {/* <FlatList data={''} keyExtractor renderItem={({item})=> }/> */}
    </View>
  );
};
